# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Separate nested checkout used for another task; it is not part of this repo.
/work/
/.worktrees/
# Historical pre-frame masters are evidence candidates for R2 archival, not
# runtime catalog inputs; do not add new snapshots to the application repo.
/content/generated/masters/history/
/content/generated/catalog-grids/

# Canonical catalog binaries are durable R2 objects, not source files. Keep
# only the budgeted 1200px pixel previews in Git for local builds and QA.
# Keep manifests, scripts and metadata in Git; retain the separately tracked
# pre-frame history masters until their own archival decision is complete.
/content/generated/masters/*.png
/content/generated/covers/*.png
/public/assets/catalog/generated/*
!/public/assets/catalog/generated/*-1200px-pixel.png

dist
dist-ssr
*.local
.env
.env.*
!.env.example
server/splint.db.bin
server/splint-preview.db.bin
server/*.db.bin
*.sqlite
*.sqlite3
server/uploads/
uploads/
server/backups/
backups/
minio-data/
.minio/
server/*.log
assets/*.pid
assets/*_pid.txt
.run/
.logs/
.e2e-runtime/
.e2e-*.pid
.e2e-*.err
test-results/
playwright-report/
blob-report/
coverage/
*.trace.zip
*.webm
*.mp4
*.dump
*.dump.sha256
.test-tmp/
server/.test-tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
